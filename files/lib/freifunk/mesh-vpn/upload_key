#!/bin/sh

UPLOAD_URL=$1
MAC=${2//:/}
KEY=`/usr/sbin/fastd --machine-readable --show-key -c /tmp/fastd/secret.mesh_vpn.conf`
WGETC=`which wget`

while [ 1 = 1 ]
do
        WGETC -q --post-data "nodeid=${MAC}&key=${KEY}" $UPLOAD_URL -O - && break
	sleep 30
done
